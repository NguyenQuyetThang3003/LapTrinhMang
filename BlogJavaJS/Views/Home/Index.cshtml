@{
    ViewData["Title"] = "Trang chủ - NightFury Blog";
}

<style>
    body {
        background: url('/images/main.jpg') no-repeat center center fixed;
        background-size: cover;
        font-family: 'Segoe UI', sans-serif;
        color: #fff;
        margin: 0;
        padding: 0;
    }

    /* ---------- HERO SECTION ---------- */
    .hero {
        text-align: center;
        padding: 160px 20px 120px;
        background: rgba(0, 0, 0, 0.55);
        backdrop-filter: blur(4px);
        border-radius: 16px;
        max-width: 1000px;
        margin: 80px auto;
        box-shadow: 0 15px 45px rgba(0,0,0,0.4);
        animation: fadeIn 1s ease-out;
    }

    .intro-text {
        font-size: 1.3rem;
        color: #e5e7eb;
        margin-bottom: 12px;
        animation: floatUp 1s ease-out;
    }

    .typing {
        display: inline-block;
        border-right: 3px solid #a5b4fc;
        white-space: nowrap;
        overflow: hidden;
        animation: typing 4s steps(40, end), blink .7s infinite;
        font-weight: 700;
        color: #a5b4fc;
        font-size: 2.2rem;
    }

    .hero p {
        font-size: 1.1rem;
        color: #d1d5db;
        margin-top: 24px;
        max-width: 750px;
        margin-left: auto;
        margin-right: auto;
        line-height: 1.8;
    }

    .btn-main {
        background: linear-gradient(90deg, #6366f1, #06b6d4);
        color: #fff;
        padding: 14px 32px;
        font-size: 1.1rem;
        border-radius: 50px;
        text-decoration: none;
        font-weight: 600;
        box-shadow: 0 6px 15px rgba(0,0,0,0.3);
        transition: all 0.25s ease;
        margin-top: 20px;
        display: inline-block;
    }

    .btn-main:hover {
        transform: translateY(-4px);
        box-shadow: 0 10px 25px rgba(0,0,0,0.4);
    }

    /* ---------- FEATURED SECTION ---------- */
    .featured {
        text-align: center;
        padding: 60px 20px 100px;
        background: rgba(255, 255, 255, 0.1);
        border-top: 1px solid rgba(255,255,255,0.2);
        border-bottom: 1px solid rgba(255,255,255,0.2);
        backdrop-filter: blur(4px);
    }

    .featured h2 {
        color: #fff;
        font-size: 2rem;
        font-weight: 700;
        margin-bottom: 16px;
        text-shadow: 0 2px 10px rgba(0,0,0,0.5);
    }

    .featured p {
        color: #d1d5db;
        max-width: 700px;
        margin: 0 auto;
        font-size: 1.05rem;
    }

    footer {
        color: #d1d5db;
        font-size: 0.9rem;
        text-align: center;
        padding: 20px;
        border-top: 1px solid rgba(255,255,255,0.1);
    }

    /* ---------- ANIMATIONS ---------- */
    @@keyframes fadeIn {
        from { opacity: 0; transform: translateY(30px); }
        to { opacity: 1; transform: translateY(0); }
    }

    @@keyframes floatUp {
        from { opacity: 0; transform: translateY(50px); }
        to { opacity: 1; transform: translateY(0); }
    }

    @@keyframes typing {
        from { width: 0; }
        to { width: 100%; }
    }

    @@keyframes blink {
        50% { border-color: transparent; }
    }
</style>

<!-- ================= HERO ================= -->
<div class="hero">
    <p class="intro-text">👋 Xin chào, tôi là <strong>Nguyễn Quyết Thắng</strong></p>
    <h1 class="typing">Rất vui được gặp bạn tại NightFury Blog!</h1>
    <p>
        Đây là không gian nhỏ nơi tôi chia sẻ những điều tôi học được về lập trình, 
        công nghệ và cuộc sống — hy vọng bạn sẽ tìm thấy điều gì đó hữu ích, 
        truyền cảm hứng hoặc đơn giản là khiến bạn mỉm cười 😊
    </p>
    <a class="btn-main" asp-controller="Posts" asp-action="Index">Khám phá bài viết</a>
</div>

<!-- ================= FEATURED ================= -->
<div class="featured">
    <h2>Bài viết nổi bật</h2>
    <p>Hãy cùng tôi khám phá những bài viết mới nhất và hấp dẫn nhất nhé!</p>
</div>

<!-- ================= FOOTER ================= -->
<footer>
    © 2025 · <strong>NightFury Blog</strong> · Kết nối & chia sẻ để cùng phát triển.
</footer>
